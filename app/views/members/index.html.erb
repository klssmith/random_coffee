<% if @members.any? %>
  <% @members.each do |member| %>
    <%= member.firstname %> <%= member.lastname %>
  <% end %>
<% else %>
  No one has signed up yet. Why not be the first?
<% end %>

<%= link_to "Join Random Coffee", "#" %>
